<h1>City Blog</h1>


<table>
  <% @articles.each do |article| %>
    <tr>
      <td><%= article.title %> &nbsp; </td> 
      <td><%= article.topic %> &nbsp; </td>
      <td><%= article.text %> &nbsp; </td>
      <td><%= link_to 'Show', article_path(article) %> &nbsp;</td>
    </tr>
  <% end %>
</table>

<%= link_to 'Admin', controller: 'articles' %>


